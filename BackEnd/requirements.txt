# Core FastAPI
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# Environment Configuration
python-dotenv==1.0.1

# Database - Supabase
supabase==2.9.1
postgrest==0.17.2
storage3==0.8.1
realtime==2.0.5
supabase-auth==2.8.4
supabase-functions==2.5.1

# Database ORM (optional, if using SQLAlchemy with Supabase)
SQLAlchemy==2.0.35
psycopg2==2.9.9  # PostgreSQL adapter for Windows
# OR use asyncpg for async operations
# asyncpg==0.29.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
PyJWT==2.9.0
cryptography==43.0.1

# HTTP Requests (for external APIs)
httpx==0.27.2
aiohttp==3.10.5
requests==2.32.3

# CORS (if needed for frontend communication)
python-multipart==0.0.12

# Data Validation & Parsing
python-dateutil==2.9.0.post0
email-validator==2.2.0

# Deployment (Vercel)
mangum==0.17.0  # ASGI adapter for AWS Lambda/Vercel